streamlit==1.37.0
python-docx==1.1.0
pymupdf==1.24.2          # for PDF text extraction (fitz)
python-pptx==0.6.23      # for PPTX extraction
pandas==2.2.2            # for curriculum tables / CSV use
numpy==1.26.4            # dependency pin for pandas
pillow==10.3.0           # used by pptx/docx for images
